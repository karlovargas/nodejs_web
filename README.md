# NOTAS
Este es mi primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Muestra los datos en consola         [boolean] [default: false]
  -h, --hasta    Imprime la tabla hasta un limite definido por el usuario
                                                          [number] [default: 10]

```